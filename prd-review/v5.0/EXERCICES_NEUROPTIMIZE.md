# Exercices de Remédiation Cognitive Neuroptimize

> **Inspirés de Cogmed, optimisés pour la démo, avec l'identité Neuroptimize**

---

## Philosophie de Design

**Cogmed = Gold Standard** → On garde les mécanismes validés (séquence → mémorisation → reproduction)

**Neuroptimize = Éducation + Esthétique Neuro** → On ajoute :
- Vocabulaire neuroscientifique vulgarisé
- Feedback sur les mécanismes cérébraux
- Esthétique "réseau neural" (pas juste des jetons)

---

## Exercices Proposés pour le POC

### 🧠 Exercice 1 : Le Réseau Neural (Mémoire Visuo-Spatiale)

**Inspiré de :** Grille Simple Cogmed
**Cible :** Mémoire de travail visuo-spatiale + Attention sélective

#### Description Visuelle

**Fond :** Dégradé sombre (bleu nuit → noir) évoquant le cerveau
**Centre :** Une grille de 4x4 "neurones" (cercles interconnectés par des lignes fines grises)
**Animation :** Quand un neurone s'active, il pulse en bleu électrique + onde de propagation

#### Fonctionnement

**Phase 1 : Activation (Encodage)**
```
┌─────────────────────────┐
│   ○───○───○───○         │
│   │   │   │   │         │
│   ○───●───○───○  ← Neurone 2 s'active (pulse bleu)
│   │   │   │   │         │
│   ○───○───●───○  ← Neurone 7 s'active (pulse bleu)
│   │   │   │   │         │
│   ○───○───○───○         │
└─────────────────────────┘

Séquence : [2, 7, 11, 3, 15] (5 neurones)
Durée : 1s par activation + 0.5s pause
Audio : "Bip" synthétique à chaque activation
```

**Phase 2 : Mémorisation (Délai)**
```
Max : "Mémorise bien cette séquence.
       Ton cerveau encode maintenant ces informations."

[Délai de 2 secondes - tous neurones gris]
```

**Phase 3 : Rappel (Reproduction)**
```
Max : "À toi ! Clique sur les neurones dans le même ordre."

[L'utilisateur clique sur les neurones]
[Feedback immédiat : Vert si correct, Rouge si erreur]
```

**Phase 4 : Feedback Neuro**

**Si Succès (100%) :**
```
Max : "Excellent. Tu as reproduit la séquence sans erreur.

       L'hippocampe (centre de la mémoire) a encodé la séquence spatiale
       et l'a maintenue active en mémoire de travail.

       Passons au niveau suivant : 6 éléments."
```

**Si Erreur Partielle (60-80%) :**
```
Max : "Bien. Tu as mémorisé X éléments sur 5.

       La séquence complète était : [Affiche la bonne séquence en bleu]

       La mémoire de travail se renforce avec la répétition.
       Veux-tu réessayer ?"
```

**Si Échec (<60%) :**
```
Max : "Tu as mémorisé X éléments sur 5.

       La séquence était : [Affiche la bonne séquence]

       Je réduis la difficulté à 4 éléments pour adapter l'exercice."
```

#### Progression Adaptative

| Niveau | Nombre de Neurones | Grille | Difficulté |
|--------|-------------------|--------|------------|
| 1 | 3 | 4x4 | Débutant |
| 2 | 4 | 4x4 | Facile |
| 3 | 5 | 4x4 | Moyen |
| 4 | 6 | 4x4 | Difficile |
| 5 | 7 | 5x5 | Expert |

**Règle :**
- 2 succès consécutifs → +1 neurone
- 1 échec → -1 neurone (min 3)

---

### 🗺️ Exercice 2 : La Carte Mentale (Mémoire Sémantique + Séquentielle)

**Inspiré de :** La Roue Cogmed + Suite de Lettres
**Cible :** Mémoire de travail verbale + Associations sémantiques

#### Description Visuelle

**Fond :** Blanc épuré (style mind map professionnel)
**Centre :** Un mot-clé central (ex : "TRAVAIL")
**Autour :** 8 concepts connexes disposés en étoile

```
          RÉUNION
             │
    EMAIL───TRAVAIL───PROJET
             │
          DEADLINE
```

#### Fonctionnement

**Phase 1 : Activation (Encodage)**
```
Max : "Voici une carte mentale autour du concept TRAVAIL.
       Observe bien le chemin de pensée."

[Animation séquentielle]
TRAVAIL → PROJET (pulse vert 1s)
PROJET → DEADLINE (pulse vert 1s)
DEADLINE → STRESS (pulse vert 1s)
STRESS → PAUSE (pulse vert 1s)

Séquence : [PROJET, DEADLINE, STRESS, PAUSE]
Audio : Voix synthétique énonce chaque mot
```

**Phase 2 : Tâche Distractive (Interférence)**
```
Max : "Pendant que ton cerveau consolide ces liens...
       Dis-moi, comment te sens-tu aujourd'hui ?"

[Boutons]
┌──────────────────┐
│ 😊 Concentré     │
├──────────────────┤
│ 😰 Stressé       │
├──────────────────┤
│ 😴 Fatigué       │
└──────────────────┘
```

**Phase 3 : Rappel (Reproduction)**

**Mode A : Ordre Direct (Facile)**
```
Max : "Redonne-moi le chemin de pensée dans l'ordre."

[L'utilisateur clique sur les mots dans l'ordre]
```

**Mode B : Ordre Inverse (Difficile)**
```
Max : "Maintenant, redonne le chemin à l'ENVERS
       (du dernier au premier)."

[Séquence attendue : PAUSE → STRESS → DEADLINE → PROJET]
```

**Phase 4 : Feedback Neuro**

**Si Succès (Ordre Direct) :**
```
Max : "Très bien. Tu as reproduit la séquence correctement.

       La zone du langage de ton cerveau a maintenu
       ces informations en mémoire active."
```

**Si Succès (Ordre Inverse) :**
```
Max : "Excellent. Reproduire une séquence à l'envers est difficile.

       Tu as utilisé la zone de planification de ton cerveau
       pour manipuler mentalement les informations."
```

#### Progression Adaptative

| Niveau | Nombre de Mots | Mode | Difficulté |
|--------|---------------|------|------------|
| 1 | 3 | Direct | Débutant |
| 2 | 4 | Direct | Facile |
| 3 | 5 | Direct | Moyen |
| 4 | 4 | Inverse | Difficile |
| 5 | 5 | Inverse | Expert |

---

### 🎯 Exercice 3 : Le Flux Attentionnel (Attention Sélective + Inhibition)

**Inspiré de :** Les Astéroïdes Cogmed
**Cible :** Attention soutenue + Inhibition des distracteurs

#### Description Visuelle

**Fond :** Gris neutre
**Centre :** Des "tâches" (carrés) flottent lentement dans un cadre
- **Tâches Cibles** : Bleu clair (ex : "Rapport", "Email", "Call")
- **Distracteurs** : Gris foncé (ex : "Pause Café", "Notification")

```
[Animation continue de dérive lente]

    ┌─────────────────────────┐
    │   [Rapport]    [☕]      │  ← Dérive lente
    │                          │
    │      [Email]             │
    │             [📱]         │
    │   [Call]                 │
    └─────────────────────────┘
```

#### Fonctionnement

**Phase 1 : Activation (Encodage)**
```
Max : "Des tâches vont s'allumer. Ignore les distracteurs."

[Séquence]
- "Rapport" pulse en bleu intense (1s)
- [Pause 0.5s - Tout flotte]
- "Email" pulse en bleu intense (1s)
- "☕" pulse en rouge (DISTRACTEUR - à ignorer)
- "Call" pulse en bleu intense (1s)

Séquence cible : [Rapport, Email, Call]
```

**Phase 2 : Rappel (Reproduction)**
```
Max : "Clique sur les TÂCHES qui se sont allumées en bleu.
       Attention aux distracteurs !"

[L'utilisateur doit suivre les carrés en mouvement et cliquer]
```

**Phase 3 : Feedback Neuro**

**Si Succès + Aucun Distracteur Cliqué :**
```
Max : "Excellent. Tu as ignoré tous les distracteurs.

       Ton cerveau a filtré efficacement les informations non pertinentes.
       C'est un mécanisme clé de la concentration."
```

**Si Erreur (Distracteur Cliqué) :**
```
Max : "Tu as cliqué sur un distracteur (élément gris).

       La capacité à ignorer les informations non pertinentes
       se développe avec l'entraînement régulier."
```

---

## Recommandations pour le POC (Vendredi)

### Priorité P0 (Must Have)

**Exercice 1 : Le Réseau Neural**
- ✅ Simple à implémenter (grille 4x4)
- ✅ Visuellement impactant (animations Framer Motion)
- ✅ Feedback neuro clair (hippocampe, cortex préfrontal)
- ⏱️ **Temps de dev estimé : 6h**

### Priorité P1 (Nice to Have)

**Exercice 2 : La Carte Mentale**
- ⚠️ Plus complexe (layout circulaire)
- ✅ Montre l'originalité (sémantique + séquentiel)
- ⏱️ **Temps de dev estimé : 4h**

### Priorité P2 (Bonus)

**Exercice 3 : Le Flux Attentionnel**
- ❌ Trop complexe pour 2 jours (mouvement continu)
- 💡 Garder pour V2

---

## Comparaison Cogmed vs Neuroptimize

| Aspect | Cogmed | Neuroptimize |
|--------|--------|--------------|
| **Esthétique** | Jeux vidéo enfantin (îles, jetons) | Neuro-moderne (réseaux, dégradés) |
| **Vocabulaire** | Neutre ("lampes", "cubes") | Éducatif ("neurones", "cortex") |
| **Feedback** | Score brut (5/7 correct) | Explication neuro ("hippocampe a encodé") |
| **Public** | Enfants + Adultes (TDAH) | Adultes (professionnels, patients) |
| **Ton** | Ludique | Professionnel bienveillant |

---

## Implémentation Technique (Réseau Neural)

### State Management

```typescript
// types/exercise.ts
interface NeuralNetworkExercise {
  level: number;           // 1-5
  gridSize: 4 | 5;         // 4x4 ou 5x5
  sequenceLength: number;  // 3-7 neurones
  sequence: number[];      // Ex: [2, 7, 11, 3, 15]
  userSequence: number[];  // Ce que l'utilisateur a cliqué
  stage: "idle" | "encoding" | "delay" | "recall" | "feedback";
}
```

### Composant React

```typescript
// components/exercises/NeuralNetwork.tsx
export function NeuralNetwork() {
  const [state, setState] = useState<NeuralNetworkExercise>({
    level: 1,
    gridSize: 4,
    sequenceLength: 3,
    sequence: [],
    userSequence: [],
    stage: "idle"
  });

  // Générer séquence aléatoire
  const generateSequence = (length: number) => {
    const max = state.gridSize * state.gridSize;
    const sequence: number[] = [];
    while (sequence.length < length) {
      const random = Math.floor(Math.random() * max);
      if (!sequence.includes(random)) sequence.push(random);
    }
    return sequence;
  };

  // Afficher la séquence (animation)
  const playSequence = async () => {
    setState(s => ({ ...s, stage: "encoding" }));
    for (let neuronId of state.sequence) {
      await activateNeuron(neuronId); // Pulse bleu + son
      await delay(500); // Pause
    }
    setState(s => ({ ...s, stage: "delay" }));
    await delay(2000); // Délai de consolidation
    setState(s => ({ ...s, stage: "recall" }));
  };

  // Gérer le clic utilisateur
  const handleNeuronClick = (neuronId: number) => {
    if (state.stage !== "recall") return;

    const newUserSeq = [...state.userSequence, neuronId];
    setState(s => ({ ...s, userSequence: newUserSeq }));

    // Vérifier si fini
    if (newUserSeq.length === state.sequence.length) {
      checkAnswer(newUserSeq);
    }
  };

  // Vérifier la réponse
  const checkAnswer = (userSeq: number[]) => {
    const correct = userSeq.every((id, i) => id === state.sequence[i]);
    setState(s => ({ ...s, stage: "feedback" }));

    if (correct) {
      // Augmenter difficulté
      setState(s => ({
        ...s,
        sequenceLength: Math.min(s.sequenceLength + 1, 7)
      }));
    } else {
      // Réduire difficulté
      setState(s => ({
        ...s,
        sequenceLength: Math.max(s.sequenceLength - 1, 3)
      }));
    }
  };

  return (
    <div className="neural-network">
      <div className="grid grid-cols-4 gap-4">
        {Array.from({ length: 16 }).map((_, i) => (
          <Neuron
            key={i}
            id={i}
            isActive={state.stage === "encoding" && /* ... */}
            onClick={() => handleNeuronClick(i)}
          />
        ))}
      </div>

      {state.stage === "feedback" && (
        <FeedbackPanel
          correct={/* ... */}
          explanation="Ton hippocampe a bien encodé..."
        />
      )}
    </div>
  );
}
```

### Animation Framer Motion

```typescript
// components/exercises/Neuron.tsx
import { motion } from "framer-motion";

export function Neuron({ id, isActive, onClick }: NeuronProps) {
  return (
    <motion.div
      className="neuron"
      onClick={onClick}
      animate={isActive ? {
        scale: [1, 1.2, 1],
        backgroundColor: ["#gray", "#00f", "#gray"],
      } : {}}
      transition={{ duration: 1 }}
    >
      {/* SVG cercle + lignes de connexion */}
    </motion.div>
  );
}
```

---

## Budget Temps pour Vendredi

| Tâche | Durée |
|-------|-------|
| Setup Exercice 1 (Réseau Neural) | 2h |
| Animation + Son | 2h |
| Logique Adaptative | 1h |
| Feedback Neuro | 1h |
| **Total Exercice 1** | **6h** |
| | |
| *(Optionnel) Exercice 2 (Carte Mentale)* | 4h |
| **Total Max** | **10h** |

---

**Conclusion :** L'Exercice 1 (Réseau Neural) est **faisable en 6h** et apporte déjà une forte identité Neuroptimize. C'est le choix le plus sûr pour la démo de vendredi.
